# Public URL where the separately deployed chatbot server is running.
# Local development defaults to http://localhost:3000 when this is omitted.
VITE_CHATBOT_URL=https://your-chatbot-service.example.com